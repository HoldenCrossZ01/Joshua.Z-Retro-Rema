using UnityEngine;

public class Test : MonoBehaviour
{
    public int coinCount; // Int's represent WHOLE NUMBERS
    public float xPosition; //Float's represent DECIMAL NUMBERS
    public string studentName; // String's represent WORDS OR PHRASES
    public bool enemyIsDead; // Bool's represent a TRUE OR FALSE VALUE
    public Vector2 playerPositionLoc; // V2 represent a 2D NUMBER

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
